# Iteration backlog

## Current priorities after the channel explorer

Completed in the explorer pass: 12 channel definitions and source rails, six browser-inspected X originals, thumbnails and provenance, raw Views/Likes, YouTube detail retrieval, English filtering, publication/observation horizon, saved navigation, responsive layout and isolated static preview packaging. The older checklist below is retained as planning history; these completed items supersede its corresponding gaps.

1. Approve and publish the isolated recorded preview to the named Vercel workspace; verify the actual hosted URL before calling deployment complete.
2. Add source-content review and a small human-rated evaluation set before changing model weights. Label relevance, factual support, reaction existence, originality, redundancy and appropriate channel separately. A platform logo or high view count is not authority.
3. Add explainable ranking as eligibility gates followed by editorial scoring: topic and time match, language/accessibility, source review status; then passenger relevance, informational novelty, perspective diversity, temporal fit and continuity with the selected channel. Persist feature values, missingness, rule/model version and the concrete evidence behind each decision. Keep raw provider Views/Likes visible as observations rather than an invented composite engagement score.
4. Evaluate ranking against human preferences and difficult negatives (reuploads, unrelated memes, foreign-language metadata, unsupported brand claims, later evidence). Track duplicate rate, supported-claim precision and channel coverage internally; do not show those as media engagement. Tune bounded weights or prompts first; model fine-tuning is justified only after a stable labeled corpus and held-out improvement.
5. Implement channel lineage and contextual axes explicitly: time is publication/observation; branch is reaction type (competitor, creator, meme, business); intent is passenger relevance; depth is follow-up continuity. Current map positions are editorial layout, not a numerical semantic embedding.
6. Add a durable hosted backend, asynchronous Astra steering capability checks and a genuinely recorded editorial replay. Only then add approved continuous playback/queue budgets and the final presentation pacing.



## 0 — Complete local baseline verification

- Completed September 10: installed baseline, Flutter resolution/analysis/widget test/release build, fixture and live browser checks, failure retention and restart persistence. See ENVIRONMENT-READINESS.md.
- Completed September 10: all three keys preserved and both YouTube slots plus gpt-6-astra passed live checks. Live programming selected HOLD/KILL only; obtain verified evidence before expecting a presentable NOW/NEXT queue.
- Add an HTTP integration test suite and a TypeScript compiler check; current Node tests exercise providers and state.

## 1 — Evidence and editorial meaning

- Add Apple primary sources and corroborated reporting alongside YouTube candidates.
- Implement VERIFY and independent source/content classification; retain unknown scores explicitly.
- Fetch video details for durations and measured statistics only when needed. Define refresh/deletion handling before retaining API data long term.
- Verify original meme and Samsung sources; make unavailable beats conditional.
- Add stable queue entries with durations fitting the passenger's eight-minute window.

## 2 — Continuing mission

- Add asynchronous SCOUT/VERIFY/MEME WATCH/CLUSTER/PROGRAM/TRAFFIC CONTROL roles within the backend.
- Implement and test Responses API conversation continuity, async tools, and mid-turn steering using actual API credentials.
- Add bounded background polling, cancellation, budget limits, accepted revision streaming, and reconnect/resync.
- Replace snapshot-plus-log persistence with a transactional event model before authentic replay.

## 3 — StellarSlate

- Evolve the basic orbital layout into the cinematic 2.5D timeline; add cluster lineage, splitting/merging, NOW frontier, and meaningful visual encoding.
- Bind targeted artifact/cluster/queue paths with atomic revision application and retain animation state.
- Resolve observed star-label collisions at desktop/1024×768, normalize HTML entities in metadata, and explain all-HOLD/KILL empty queues.
- Add recognizable media previews, an accessible visual key, keyboard selection, and fuller reduced-motion behavior.
- Test desktop/iPad landscape sizes and measure release frame timing before performance claims.

## 4 — Timeline, replay, submission

- Implement source-grounded aftershock injection, context mutation, and historical scrubbing.
- Label contemporaneous snapshots vs reconstructed history and projections.
- Record real decisions/evidence for explicit replay and derive all counters from accepted events.
- Rehearse the six-beat 60-second narrative, prepare screenshots/video, verify third-party attribution, and publish the reviewed public repository.

Native macOS/iPad packaging and public authenticated hosting remain deferred.
